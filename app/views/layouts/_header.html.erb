<nav class=" container-fluid navbar bg-dark" data-bs-theme="dark">
        
          <% if user_signed_in? %>
            <div class="container-fluid">
              <h2><%= link_to "Members Only", root_path, class: "navbar-brand"%></h2>
          
                <div class="justify-content-left">
                      <%= link_to current_user.email, edit_user_registration_path, class: "nav-link"%>
                  <%= link_to "Add new Post", new_post_path, class: "btn btn-outline-success" %>
                  <%= link_to "Log Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-outline-danger" %>
                </div>
            </div>

             

          <% else %>
            
            <div class="container-fluid">
              <h2><%= link_to "Members Only", root_path, class: "navbar-brand"%></h2>
                  <%= link_to "New post", new_post_path, class: "btn btn-outline-success grid g-col-1"%>
              <div class="justify-content-left">
                <%= link_to "Sign In", new_user_session_path, class: "btn btn-outline-primary" %>
                <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-warning" %>
              </div>
            </div>
          <% end %>
      
</nav>
